#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npm run build

npm outdated | grep -v Package | awk '{print $1}' | xargs -I {} npm install {}@latest --legacy-peer-deps
git add package.json package-lock.json
git commit -m "build(deps): upgrade dependencies"

